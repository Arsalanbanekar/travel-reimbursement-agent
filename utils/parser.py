"""
Claim and receipt parsing utilities.

Future purpose:
- Parse Streamlit form input and uploaded receipt metadata into ClaimInput schema.
- Parse sample_claims.json entries for batch testing.
- Extract receipt fields (date, amount, category, vendor, attachment status)
  from receipts.json or uploaded files.

Assignment mapping:
- Claim intake from form/JSON (Section 3).
- Mock receipt metadata handling (Section 4).
"""
